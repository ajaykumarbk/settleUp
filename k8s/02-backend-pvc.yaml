apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: splitwise-backend-pvc
  namespace: splitwise
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 5Gi
